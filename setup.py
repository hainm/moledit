#!/usr/bin/env python

import os
import sys
from setuptools import setup

version = '0.0.1'

setup(
    name='moledit',
    version=version,
    zip_safe=False,
    description='4fun',
    author='Hai Nguyen',
    packages=['moledit'])
